# ParaMed AI Configuration

# Inference mode: "vertex" (Vertex AI), "local" (HuggingFace), "auto" (try vertex, fallback local)
INFERENCE_MODE=auto

# === Vertex AI Settings (online mode) ===
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=europe-west4
GCP_SERVICE_ACCOUNT_KEY=path/to/service-account.json
VERTEX_MODEL=medgemma-1.5-27b-it
# VERTEX_ENDPOINT_ID=  # Optional: custom endpoint

# === Local HuggingFace Settings (offline mode) ===
MEDGEMMA_MODEL=google/medgemma-1.5-4b-it
QUANTIZE_MODEL=true
DEVICE=cuda
MAX_NEW_TOKENS=2048

# === STT ===
WHISPER_MODEL=openai/whisper-small

# === RAG ===
CHROMA_DB_PATH=./app/data/embeddings/chroma_db
EMBEDDING_MODEL=all-MiniLM-L6-v2
RAG_TOP_K=5

# === Server ===
HOST=0.0.0.0
PORT=8080
